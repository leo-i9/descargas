services:
- type: web
  name: my-web-service
  env: node
  buildCommand: npm install 
  startCommand: node main.js
  envVars:
  - key: NODE_ENV
    value: production
  - key: MY_SECRET
    fromSecret: mysecret
  autoDeploy: true
